# this is the main report generating function

import json
import Generate
import Validate
import Compile

def Monthly_Report_Generation(month):
    pass # Validate and Compile  - do we need this or compile level is enough?
